# I1 — Contract compatibility — evidence (archived, owner: serving-contracts)

**Archived:** 2026-07-12 (IL-T008 consistency pass), by inference-lab's evidence-archivist
duty (`docs/integration.md`). I1 is re-entrant: it re-runs on every contract release. This
checklist archives the **latest** re-run (the v1.0.0 freeze, the most demanding one so far —
MAJOR classification, prerequisite for I6) plus a pointer to the release history of prior
runs. No file in this directory is generated by inference-lab; everything here is a citation
(path + commit) into `serving-contracts`, per the "this repo composes, never re-derives
another repo's runtime output" rule.

## Acceptance mapping (`docs/07-integration-milestones.md` I1 / `docs/integration.md`)

| Criterion | Status | Evidence (path + commit) |
|---|---|---|
| All four consumers (infergate, inferbench, fleetlab, inferops) validate the golden fixtures **and their own emitted artifacts** against the same bundle tag | **PASS** — GREEN across all four, 2026-07-12 re-run | `serving-contracts` commit `507208b25737470b9eb2f9553a5c55f8f535f1d5`, `RELEASES.md` §"I1 re-run on the frozen bundle (SC-T010 stop condition; run 2026-07-12)" |
| Unsupported-field rejection cases covered | **PASS** (part of the kit selftest, unchanged since v0.1.0) | `serving-contracts@507208b`, `RELEASES.md` §"Validate": `kit/contracts-validate.py selftest` → 12 schemas meta-validated, **52/52 positives, 29/29 negatives failed-as-required, GREEN, exit 0** |
| Same bundle tag referenced by all four | **PASS** | all four re-validated against tag `v1.0.0` (commit `507208b`) in the same session, per consumer detail below |
| Re-entrant: re-runs on every release | **PASS** (this is re-run #2 in the archived record; #1 happened at v0.1.0/v0.2.0 per each consumer's own CI wiring, not independently re-verified in this archive until now — see "Honesty note" below) | — |

## Per-consumer detail (2026-07-12 re-run, cited from `serving-contracts@507208b` `RELEASES.md`)

| Consumer | Pinned tag (as recorded in the consumer's own docs) | Artifacts re-validated against the frozen v1.0.0 bundle | Result |
|---|---|---|---|
| infergate | `v0.2.0` (`484b449`, `docs/integration.md`) | `deploy/infergate.deployment-contract.json`, `deploy/mock-backend.deployment-contract.json` → 2/2 PASS vs `deployment-contract`; `internal/backend/llamacpp/llamacpp.backend-capability.json`, `internal/mockengine/mock.backend-capability.json` → 2/2 PASS vs `backend-capability` | **GREEN** |
| inferbench | `v0.2.0` (`484b449`, `docs/implementation-notes.md` IB-T010) | `docs/evidence/ib-t010/`: 29/29 `benchmark-run` manifests, 29/29 `raw-event` JSONL, 6/6 `workload`, 3/3 `slo`, 9/9 `benchmark-result` — all PASS | **GREEN** |
| fleetlab | `v0.2.0` (`484b449`, vendored `vendor/serving-contracts-v0.2.0/`, `docs/tasks.md` FL-T002) | `examples/recommendations/e2-admission-sane-v1-5x-scaleout.capacity-recommendation.json` → 1/1 PASS vs `capacity-recommendation`; `tests/golden/fixtures/real/slo/scenario-b-llamacpp-cpu-shakedown.slo.json` → 1/1 PASS vs `slo` | **GREEN** |
| inferops | `v0.2.0` (bootstrap note, `docs/implementation-notes.md`) | live-gateway smoke capture `scripts/evidence/smoke-20260711T232118Z/`: `chat-completion-response.json`, `models-response.json`, 3× error-response fixtures, `chat-completion-stream.sse` → 6/6 PASS across `api.chat-completion-response`/`api.models-response`/`api.error-response`/`api.stream-event` | **GREEN** |

**Overall: I1 GREEN across all four consumers on the frozen `v1.0.0` bundle** — no consumer
needed any change to stay green (the v0.2.0→v1.0.0 diff is two description-only wording
clarifications, zero shape/validation-outcome changes). All four consumers' own recorded pin
strings still read `v0.2.0`/`484b449` as of this date; moving each consumer's own recorded pin
string to `v1.0.0` is each repo's own follow-up (not required for I6, since Contract 7 —
capacity-recommendation — is unchanged by the freeze; independently confirmed:
`git diff v0.2.0 v1.0.0 -- schemas/capacity-recommendation.schema.json` in `serving-contracts`
produces no output).

## Honesty note — re-entrancy history

This archive was created at IL-T008 (2026-07-12), after I2–I7 evidence already existed. Prior
contract releases (`v0.1.0` 2026-07-10, `v0.2.0` 2026-07-11) each carry their own "I1 tracked
per consumer" language in `serving-contracts/RELEASES.md`, but inference-lab did not
independently archive a per-consumer I1 evidence snapshot at those earlier releases (a gap in
IL-T008's ongoing-upkeep discipline, corrected now). The **v1.0.0 re-run above is the first
I1 snapshot independently cross-checked and archived here**, and it is the most rigorous to
date (real emitted artifacts, not only fixtures, for all four consumers). Nothing before this
date is claimed as archived I1 evidence in this repository — the compatibility matrix's I1 row
(`compatibility/matrix.md`) cites only this 2026-07-12 snapshot.

## Pins referenced (already recorded in `pins/pins.yaml`, no new pin added by this archive)

- `contracts-bundle-v1-0-0` (serving-contracts `v1.0.0`, tag object `64c3205b…`, commit
  `507208b25737470b9eb2f9553a5c55f8f535f1d5`) — `proven_at` already includes `I6`; this
  archive additionally grounds the matrix's new `I1` row in the same tag, no pin edit needed.

## Re-run trigger

Every future contract release re-runs I1; a MAJOR release re-run is a prerequisite before any
scenario is re-claimed at the new bundle version (`docs/integration.md` "Re-run triggers").
The next I1 snapshot archived here supersedes this one with a new dated entry (ADR-0002 —
this file is not rewritten, only superseded).
