# I2 — Local request path: executed acceptance checklist (Scenario A, IL-T002)

Executed 2026-07-10 against the composed Scenario A stack. All numbers **measured**
(provenance: this run; raw artifacts referenced per item, all under `evidence/i2/`).
Orchestration: `scenarios/a/{build.sh,compose.yaml,run.sh}`; every number below is
recomputable from the raw artifacts via `scenarios/a/checks.py` (outputs archived in
`logs/checks.log`).

**Overall: all executed criteria PASS; one criterion is a documented DEVIATION
(PostgreSQL usage write — D-001). I2 acceptance review by the user is pending.**

| # | Criterion | Outcome | Evidence |
|---|---|---|---|
| 1 | Components brought up from pinned versions only; digests logged; pins validator green | **PASS** (with deviations D-002/D-003 on *how* images were produced) | `pins-snapshot.yaml`; `logs/build-info.txt` (pins, toolchain, binary sha256s, image IDs); `logs/run.log` (compose ps); validator output in IL-T002 commit |
| 2 | Seeded workload run: `chat-short`, seed + workload version recorded | **PASS** — canonical `chat-short` v1.0.0, seed 1003001; 4830 sent / 4830 ok / 0 errors / 0 shed; max send slip 67.1 ms (< 100 ms watchdog → run valid); wall 10m04s | `raw/runs/chat-short/{manifest.json,events.jsonl.gz,run.log}` |
| 3 | 100 concurrent streams, zero frame mixing | **PASS** — max **154 concurrent** in-flight streams (≥100 required); 2736/2736 events ok; 0 integrity violations (per-stream server-reported usage tokens == client-received content chunks; SSE framing parsed clean, terminal `[DONE]` on every stream). Gateway-side reference: infergate IG-T003/G2 frame-integrity evidence (zero mixing, monotonic ids at 100 streams) at the pinned commit 5d69aeb | `raw/runs/concurrency-100/`; `logs/checks.log` (integrity check); infergate@5d69aeb `docs/implementation-notes.md` (G2) |
| 4 | 3-point cancellation; mock abort observed within declared bound each time | **PASS** — declared bound **100 ms** (A-006), measured release deltas (mock `/debug/state` abort timestamp vs client cancel completion, nearest-neighbor pairing): **pre-first-token** 30/30 aborts `phase=pre_first_token, chunks_sent=0`, deltas 0.247/0.402/0.573 ms (min/med/max); **mid-stream** (cancel@8 tokens) 29 aborts `phase=mid_stream, chunks_sent≥9` + 1 short stream completed ok, deltas 0.310/0.413/0.724 ms; **near-completion** (cancel@240 of 256-token cap) 12 aborts `phase=mid_stream, chunks_sent=241` + 187 completed before trigger + **1 completion/cancel race** (mock finished writing before observing the disconnect; stream conservation exact: 188 completed + 12 aborted = 200 started — race, not leak), deltas 0.318/0.367/0.638 ms | `raw/runs/cancel-*/{events.jsonl,debug-state.json,run.log}`; `logs/checks.log` |
| 5 | Raw-event JSONL schema-valid against the pinned contracts bundle | **PASS** — all 5 runs' `events.jsonl` valid against `raw-event`, all 5 `manifest.json` valid against `benchmark-run`, all 5 workload files valid against `workload`, smoke SSE transcript valid against `api.stream-event` (kit @ serving-contracts 8d81492) | `logs/kit-validate.log` |
| 6 | Client-vs-gateway TTFT agreement within declared tolerance; measurement-tolerance statement archived | **PASS** — gateway mean **302.010 ms** (4830 observations, `inference_ttft_seconds` scrape delta), client mean **303.917 ms** (4830 ok events; p50 303.359 ms, p95 305.167 ms); difference **+1.907 ms**, inside declared tolerance **0 ≤ client−gateway ≤ 25 ms**. Measurement points: gateway = complete client request read → first upstream body byte at gateway (Contract 2); client = scheduled send (CO-safe basis) → first response body byte at client. Tolerance justification: same-host bridge transit (<1 ms RTT, measured 0.15 ms at preflight), gateway request-read/dispatch overhead, client send-slip on the scheduled-send basis (IB-T004 calibration: p50 within [−2,+15] ms). Populations match exactly (4830 == 4830) | `logs/checks.log` (ttft check incl. bucket-CDF table); `raw/metrics-00-pre-chat-short.prom`, `raw/metrics-01-post-chat-short.prom`; `raw/runs/chat-short/` |
| 7 | PostgreSQL usage write observed and consistent with raw events | **DEVIATION — not claimable (D-001)**. infergate@5d69aeb has no tenancy/usage accounting (IG-T007/T008 are Wave 3); Scenario A v1 composes **without** PostgreSQL. Nothing faked. Re-run with PostgreSQL when IG-T008 lands and a new gateway release is pinned. Note: tokens-billable-before-cancel is client-side evidenced already (canceled events retain output tokens + TTFT/ITL) | `docs/implementation-notes.md` D-001; `scenarios/a/README.md`; canceled events in `raw/runs/cancel-*/events.jsonl` |
| 8 | Traces show full gateway span sequence; metrics show canonical names | **PASS** — collector file export: 46 956 spans, 7831 traces, **7801 traces with the full `recv → queue.wait → upstream.connect → ttft → stream.relay → settle` sequence**; conservation exact: 7831 = 4830+2736+30+30+200 workload + 2 smoke + 3 flush requests, and 7831−7801 = 30 = the pre-first-token cancels (which never reach `ttft`/`stream.relay` by design). Spans carry `gen_ai.*` attributes (semconv v1.34.0). Metrics scrapes show `inference_ttft_seconds`, `inference_itl_seconds`, `inference_queue_depth`, `inference_sheds_total` (+ e2e/queue-wait/in-flight) | `raw/traces.jsonl.gz`; `raw/trace-excerpt.json` (one full-sequence trace); `raw/metrics-03-final.prom`; `logs/otel-collector.log` |
| 9 | Evidence archived: run logs, raw events, trace export, executed checklist | **PASS** — this directory (layout per `docs/interfaces.md` §4; large JSONL gzipped, noted in `notes.md`) | `evidence/i2/` |
| 10 | Reviewed (user acceptance) | **PENDING** — I2 is claimed (and `proven_at`/`milestone_evidence` in `pins/pins.yaml` filled) only after user review of this evidence, including deviation D-001 | — |
